import transform from "css-to-react-native";
const ALLOWED_STYLE_PROPERTIES = {
  color: true,
  background: true,
  backgroundColor: true,
  fontWeight: true,
  fontStyle: true
};
export const getRNStylesFromHljsStyle = hljsStyle => {
  return Object.fromEntries(Object.entries(hljsStyle).map(_ref => {
    let [className, style] = _ref;
    return [className, cleanStyle(style)];
  }));
};
export const cleanStyle = style => {
  const styles = Object.entries(style).filter(_ref2 => {
    let [key] = _ref2;
    return ALLOWED_STYLE_PROPERTIES[key];
  }).map(_ref3 => {
    let [key, value] = _ref3;
    return [key, value];
  });
  return transform(styles);
};
//# sourceMappingURL=styles.js.map